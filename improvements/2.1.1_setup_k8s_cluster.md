

Following document describes how to setup kubernetes cluster on different platforms

# Anastasia: TO-DO Validate instructions for each platform once by running the commands
# TO-DO: In progres, need to create openshift instructions, working with red hat team for streamlining usage.

1. [GCP-Google Kubernetes Enginer(GKE)](#setup-google-kubernetes-engine-gke)
2. [AWS-Elastic Kubernetes Service(EKS)](#setup-elastic-kubernetes-serviceeks)
3. [Azure-Azure Kubernetes Service(AKS)](#setup-aks-cluster)
4. [Redhat-Openshift]()
5. [Minikube]()

# Setup Google Kubernetes Engine (GKE)

The document assumes some experience with Google Cloud Platform(GCP).
For more information on GKE, see the [doc](https://cloud.google.com/kubernetes-engine/docs/quickstart).

## Prerequisites

All commands from this installation guide can be run either in the **Google Cloud shell** or in **your local shell**.

To use *Google Cloud shell*, you need nothing but a modern web browser.
If you would like to use *your local shell*, install the following:


1. [gcloud :octicons-link-external-16:](https://cloud.google.com/sdk/docs/quickstarts). This tool is part of the Google Cloud SDK. To install it, select your operating system on the [official Google Cloud SDK documentation page :octicons-link-external-16:](https://cloud.google.com/sdk/docs) and then follow the instructions.

2. Ensure you are [logged in](https://docs.cloud.google.com/docs/authentication/gcloud) and have the required privilifes.Install `gke-gcloud-auth-plugin` if not installed already.

 ``` {.bash data-prompt="$" }
    $ gcloud auth login
    $ gcloud components install gke-gcloud-auth-plugin
```
Refer [doc](https://docs.cloud.google.com/sdk/gcloud/reference/auth/login) for more info.

## Create and configure GKE cluster



1. Create a cluster

``` {.bash data-prompt="$" }
$ gcloud container clusters create <cluster-name> --project <project ID> --zone us-central1-a --cluster-version {{ gkerecommended }} --machine-type n1-standard-4 --num-nodes=3
```
Refer [doc](https://docs.cloud.google.com/sdk/gcloud/reference/container/clusters/create) for more options.
gcloud CLI properties can be configured using [gcloug config](https://docs.cloud.google.com/sdk/gcloud/reference/auth/login)
!!! note

    You must edit the above command and other command-line statements to replace the `<project ID>` placeholder with your project ID (see available projects with `gcloud projects list` command). You may also be required to edit the *zone location*, which is set to `us-central1` in the above example. Other parameters specify that we are creating a cluster with 3 nodes and with machine type of 4 vCPUs.


## Verify 

Check [section](#verify-kubernetes-cluster)


## Removing the cluster

You can clean up the cluster with the `gcloud` command as follows:

``` {.bash data-prompt="$" }
$ gcloud container clusters delete <cluster name> --zone us-central1-a --project <project ID>
```

??? note "Also, you can delete your cluster via the Google Cloud console"

    Just click the `Delete` popup menu item in the clusters list:

    ![image](assets/images/gke-quickstart-cluster-connect.svg)

The cluster deletion may take time.

!!! warning

    After deleting the cluster, all data stored in it will be lost!


# Setup Elastic Kubernetes Service(EKS)

The document assumes some experience with Amazon Web Services(AWS).
For more information on EKS, see the [doc](https://docs.aws.amazon.com/eks/latest/userguide/what-is-eks.html).

Commands below show how to create cluster using [eksctl](https://docs.aws.amazon.com/eks/latest/eksctl/what-is-eksctl.html).

## Prerequisites

1. **AWS Command Line Interface (AWS CLI)** for interacting with the different
    serv of AWS. You can install it following the [official installation instructions for your system :octicons-link-external-16:](https://docs.aws.amazon.com/cli/latest/userguide/cli-chap-install.html).

2. **eksctl** to simplify cluster creation on EKS. It can be installed
    along its [installation notes on GitHub :octicons-link-external-16:](https://github.com/weaveworks/eksctl#installation).

3. Ensure you are logged in and have priviliges to create EKS cluster and the associated AWS resources with command line. [Refer](https://docs.aws.amazon.com/cli/v1/userguide/cli-chap-authentication.html) different methods for authentication. Detailed steps for authentication is beyond the scope of this document.


## Create and configure EKS cluster



1. Create cluster with eksctl
```
eksctl create cluster --name <cluster-name>
```
Refer the [documentation](https://docs.aws.amazon.com/eks/latest/eksctl/creating-and-managing-clusters.html) for more options.

This will create the EKS cluster and add the kubeconfig to ~/.kube/config 


## Verify 

Check [section](#verify-kubernetes-cluster)

## Deleting EKS Cluster

1. Delete the cluster with the following command
```
eksctl delete cluster --name <cluster-name>
```


# Setup AKS Cluster

## Prerequisites

The following tools are used in this guide and therefore should be preinstalled:

1. **Azure Command Line Interface (Azure CLI)** for interacting with the different
    parts of AKS. You can install it following the [official installation instructions for your system :octicons-link-external-16:](https://docs.microsoft.com/en-us/cli/azure/install-azure-cli).

2. Ensure you are [logged in](https://learn.microsoft.com/en-us/cli/azure/authenticate-azure-cli?view=azure-cli-latest) and have the required priviliges 
```
az login
```

## Create and configure AKS cluster


1. Create AKS cluster
```
az aks create  --resource-group <resource-group-name> --name <cluster-name>
```
For more options, refer to the [doc](https://learn.microsoft.com/en-us/cli/azure/aks?view=azure-cli-latest#az-aks-create)

You may wait a few minutes for the cluster to be generated.

## Verify 

Check [section](#verify-kubernetes-cluster)

## Delete AKS Cluster

1. Delete the cluster
```
az aks delete  --resource-group <resource-group-name> --name <cluster-name>
```

https://learn.microsoft.com/en-us/cli/azure/aks?view=azure-cli-latest#az-aks-delete


The cluster deletion may take time.

!!! warning

    After deleting the cluster, all data stored in it will be lost!


# Create k8s cluster on Openshift    


# Create kubernetes cluster with minikube 


# Verify Kubernetes Cluster

Ensure tool `kubectl` is [installed](https://kubernetes.io/docs/tasks/tools/install-kubectl/)

1. Check if kubernetes API is accessible

```
kubectl version
```
Output should provide both client and server version

2. Check if kubectl commands are working

```
kubectl get ns
```

```
kubectl get po
```
